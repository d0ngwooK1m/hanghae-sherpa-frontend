import React from 'react';
import { Frame, LoginForm } from '../components';

const LoginPage = () => {
  return (
    <React.Fragment>
      <Frame>
        <LoginForm />
      </Frame>
    </React.Fragment>
  );
};

export default LoginPage;
