import React from 'react';
import { LoginForm } from '../components';

const LoginPage = () => {
  return (
    <React.Fragment>
      <LoginForm />
    </React.Fragment>
  );
};

export default LoginPage;
