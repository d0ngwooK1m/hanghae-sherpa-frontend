import React from 'react';

import { Frame, MainGraph } from '../components';

const MainPage = () => {
  return (
    <React.Fragment>
      <Frame>
        <MainGraph />
      </Frame>
    </React.Fragment>
  );
};

export default MainPage;
